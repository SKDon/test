@inherits Alicargo.MvcHelpers.BaseWebViewPage<DynamicScriptMethodDescription[]>
var Alicargo = (function($a){$a.Urls = (function($u){
@foreach (var item in Model)
{
	var line = string.Format("$u.{0}_{1}='{2}';", item.Controller, item.Action, Url.Action(item.Action, item.Controller));
	@Html.Raw(line)
}
return $u;}($a.Urls || {}));return $a;}(Alicargo || {}))