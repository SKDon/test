@using System.Globalization
@model SelectModel

@Html.DropDownList(Model.Name, Model.List.Select(x => new SelectListItem
{
	Text = x.Value,
	Value = x.Key.ToString(CultureInfo.InvariantCulture),
	Selected = x.Key == Model.Id
}))